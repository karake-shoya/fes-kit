# 次セッションでやること

> **下の「実機確認待ち①〜⑤」計25項目は、2026-08-08 に仕分け済み。**
> 全25項目が E2E の射程内で、実機でしか確認できないものは0件だった。
> 区分と確かめ方は [docs/2026-08-08_実機確認25項目の仕分け.md](../../docs/2026-08-08_実機確認25項目の仕分け.md)。
> **目視で消していくより、E2E を書いて機械に渡すほうが速い。**

- **採算シミュレーションは設計のステップ1〜7がすべて完了**。
  設計は [docs/採算シミュレーション設計.md](../../docs/採算シミュレーション設計.md)。
  残りは設計の「未決・保留」節（1人が複数個買う前提の扱い／パターンの引き継ぎ）のみで、
  どれも実データが出てから決める性質のもの。**次の題材は Shoya と相談してから決める**。

- **Shoya の実機確認待ち①（2026-07-29 リファクタ分）**。Clerk 認証後の画面は preview ツールで
  確認できないため、以下は実機（iPhone/PWA）で目視してほしい：
  1. タブを切り替えたとき、**下部タブバーが残ったまま**中身だけスケルトンになること
  2. 持ち物のチェック・スケジュールの状態バッジが**タップした瞬間に**変わること
  3. 機内モードにして上の2つを操作すると、画面下に「保存できませんでした」が出て表示が元に戻ること
  4. 追加・編集・削除ダイアログ（材料／商品／予定／持ち物／試作／新規プロジェクト）が
     従来どおり動くこと（共通シェルに載せ替えたため）

- **Shoya の実機確認待ち②（採算シミュレーション分）**：
  1. ホームに「かかるお金」「採算シミュレーション」のカードが増え、バッジが出ること
  2. かかるお金の追加・編集・スワイプ削除が動くこと
  3. 固定費が未登録のとき、シミュレーション画面が「かかるお金を登録する」案内になること
  4. 登録後に「全部で◯個売ればトントン」が出て、商品ごとの内訳の合計と一致すること
  5. 設定に想定来場者数を入れると購入率が出て、100%超で赤い警告になること

- **Shoya の実機確認待ち③（採算パターン分）**：
  1. シミュレーション画面に「パターンで比べる」が出て、「追加」でダイアログが開くこと
  2. ダイアログで値段・個数を打ち替えると、**保存前に**下の手残り・購入率がその場で動くこと
  3. 保存したパターンのカードに手残り・全体個数・購入率が出ること
  4. カードをタップで編集、左スワイプで削除ができること
  5. 「これにする」→確認 → 商品の値段・作る予定数が実際に書き換わり、
     画面下に「反映しました」（チェックアイコン）が出ること
  6. パターンを5件作ると「追加」ボタンが消え、上限の案内が出ること

- **Shoya の実機確認待ち④（AI診断・手残り分）**。
  > **AI往復は検証済み**（2026-07-29・使い捨てスクリプトで本番と同じスキーマ/プロンプトを7回実行）：
  > 通常条件4回はすべて `recipeId` をそのまま返し、価格は10円単位・個数は整数・
  > 全商品ぶん・重複なし・原価割れなし・購入率100%以下・黒字。
  > **赤字条件3回では、AIが3回とも「利益率を確保」等の前向きな文章を付けたまま
  > 16〜17万円の赤字案を返した** ⇒ サーバー側の赤字警告は実際に必要で、
  > 「検算結果をAIの文章より上に大きく出す」判断は実挙動で裏付けられた。
  > なお**原価割れによる `dropped` の分岐は7回とも発火せず未通過**（モデルが
  > 「価格は原価より高く」の指示を守るため）。安全網として残す。
  1. シミュレーション画面に「AIに値段と個数を相談する」が出て、押すと提案が返ること
     （`ANTHROPIC_API_KEY` 未設定ならボタンごと出ない）
  2. 提案パネルで**手残り・個数・購入率（コードの計算）が一番上に大きく**出て、
     AIの文章はその下にあること
  3. 「この案をパターンに保存する」でパターン一覧に増えること
  4. 実績ページのサマリーに「かかるお金 ¥X を引いた手残り」が出ること
  5. かかるお金が未登録のときは、代わりに登録への案内リンクになること

- **Shoya の実機確認待ち⑤（パターン比較表）**。
  > **レイアウトは検証済み**（`/` を一時差し替えてブラウザで確認・復元済み）：
  > 左列固定＋横スクロール、2件以上で開く、マーカーの二重表示なし、
  > 同額でも王冠は1つだけ。残るは実データでの見え方の確認のみ。
  比較表はスマホ幅での見え方が要になるが、この画面は Clerk 認証後のため
  preview ツールで確認できず、**私（Claude）は実物を一度も見ていない**。
  一時的な公開ルートを作る案は `src/proxy.ts` の認証設定を変える必要があり、
  CLAUDE.md の「認証バイパスは事前承認なしに行わない」に触れるため見送った。
  iPhone で以下を目視してほしい：
  1. パターンが2件以上あると「表で見くらべる」が**開いた状態**で出ること（1件なら閉じている）
  2. 三角マーカーが**二重に出ていない**こと（Safari の既定マーカーを消しているため）
  3. 左端の**商品名の列が固定**されたまま、右方向に横スクロールできること
  4. いちばん手残りが多い列に**王冠**が付くこと（「今」が最良ならそこに付く）
  5. 列が「今 → A案 → B案…」の順で、`今` が必ず左端にあること

---

## 完了済み

- **実機確認⑥⑦（ひとつ前に戻す／レビュー修正分）＝Shoya が実機で確認・正常動作**（2026-07-29）:
  - ⑥「これにする → ひとつ前に戻す」の1往復が実データで通った。
    これで **`applyScenario`（recipes を書き換える唯一の経路）が初めて実挙動で裏付けられた**
    （`actions/` はテスト0件のまま＝テストは純粋ロジックだけ、という合意による）。
    控えカードの非編集・非削除、行き来、上限とは別枠であることも含めて確認済み。
  - ⑦ パターン保存後に「追加」を開き直したときの初期値が**今のレシピの値**に戻ることを確認。

- **採算シミュレーション ステップ6〜7（AI診断・手残り）**（2026-07-29）:
  - `src/actions/ai-simulation.ts`: Claude（haiku）に**価格と個数と理由だけ**返させ、
    金額はすべてコードで計算。サーバー側で必ず検算する
    （不明IDを捨てる／10円単位に丸める／原価割れの商品だけ外す／赤字・購入率100%超を警告）。
    原価割れ判定は**プロンプトで見せた表示額（円に丸めた原価）**と突き合わせる
    （生の原価と比べると「¥100 が ¥100 を下回る」という読めない理由で外れるため）。
  - `src/components/app/ai-simulation-panel.tsx`: **検算結果を一番上・一番大きく**、
    AIの文章はその下。採用は `source: "ai"` のパターンとして保存（保存時も再検証）。
  - 実績ページのサマリーに「実績利益 − かかるお金」の**手残り**を追加。
    未登録なら登録への導線に切り替え。`revalidate.ts` の `expenses` に `results` を追加。
  - **サマリーはサーバー値**なので、カード編集中は追従せず再訪時に整合する
    （`salesRecords` はホームのみ再検証、という既存方針のまま）。

- **採算シミュレーション ステップ5（パターン）**（2026-07-29）:
  - スキーマ: `simulation_scenarios` / `simulation_items`（`drizzle/0005_*.sql`、Turso適用済み）。
  - `evaluateScenario()`（+8テスト）: 明細ではなく**今の商品を軸に走査**するため、
    削除された明細は自然に落ち、後から増えた商品は0個（`unlisted`）として並ぶ。
    原価はパターンに保存せず毎回材料マスタの最新値で再計算する。
  - `src/db/queries/scenarios.ts`（上限5件・N+1回避）、`src/actions/scenario.ts`
    （明細の入れ替えと「これにする」はどちらも `db.batch` で1トランザクション）。
  - `ConfirmDialog` を切り出し `ConfirmDeleteDialog` をその上に載せ替え、
    トーストに success トーン（チェックアイコン）を追加。
  - **0個の商品は「これにする」で値段だけ反映**し作る予定数は据え置く
    （レシピ側が1以上必須のため。確認ダイアログにもその旨を出す）。

- **採算シミュレーション ステップ1〜4**（2026-07-29）:
  - スキーマ: `project_expenses` テーブル・`projects.expected_visitors`（`drizzle/0004_*.sql`、適用済み）。
  - `src/lib/breakeven.ts`（+19テスト）: 構成比を保ったスケール、原価未登録の除外、
    全商品赤字／固定費0／作る予定数0の異常系、切り上げで利益が負に沈まないための補正。
  - かかるお金CRUD（`expenses` ページ）と採算シミュレーション画面（AIなし・計算のみ）。
  - ホームのサブ機能カード2枚追加、設定に想定来場者数、`revalidate.ts` に
    `expenses` データと `expenses` / `simulation` 画面を追加。
- **リファクタリング Phase 0〜3**（2026-07-29）:
  - Phase 0: Vitest 導入。`lib/recipe-cost` `lib/parse` `lib/schedule` `lib/checklist`
    `lib/shopping-list-calc`（買い出し積算をクエリから切り出し）に44件のテスト。
  - Phase 1: 認可（`requireProjectPage` / `requireProjectRole`）・画面再検証（`revalidateProject`）・
    スワイプ削除（`swipe-action-card`）・ダイアログ（`useEntityDialog` + `entity-form-dialog`）・
    ページ骨格（`page-shell`）を共通化。ホームのメンバーシップ二重取得も解消。
  - Phase 2: `projects/[id]/loading.tsx` と `dashboard/loading.tsx` を追加してタブバーを保持。
    ホームを Suspense 分割して初期表示を早めた。
  - Phase 3: `(app)/error.tsx`、失敗通知トースト、チェック・状態切替の楽観的更新。
- **持ち物・準備チェックリスト**（2026-07-05）: `checklist_items` テーブル・
  `/projects/[id]/checklist` ページ・買い出しリストからの一括インポート・
  ホームの「準備の進みぐあい」への合算として実装済み。
- **当日の売上・実績記録**（2026-07-05）: `sales_records` テーブル・`/projects/[id]/results` ページ・ホームの導線整理（タブバー重複カードの削除）として実装済み。

---

## 計測メモ（2026-07-29 時点）

クライアントJS合計 約1.18MB（未圧縮・32チャンク）。内訳の大きい順に
React DOM 約198KB、**Clerk 約155KB**、その他100KB級が3つ。
Clerk はルートレイアウトの `ClerkProvider` が読み込むため、
`AppHeader` の `UserButton` を別画面へ移しても削減にはならない（導線を削る意味がないので見送り）。
